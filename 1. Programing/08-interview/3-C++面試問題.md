### C++ 語言特性與底層機制
Q1: 簡述 C++11 引入的 **Move 語義 (Move Semantics)**。它如何通過右值引用和移動構造函數/賦值運算符來避免不必要的深拷貝？

Q2: 什麼是 **RAII (Resource Acquisition Is Initialization)** 原則？在 C++ 程式設計中，如何利用 RAII 來管理 Socket 連接或鎖資源？

Q3: 描述 **虛函數 (Virtual Function)** 的底層實現（vptr 和 vtable）。在高頻交易的**核心熱路徑**中，為什麼通常建議避免使用虛函數？

Q4: 比較 **`std::unique_ptr`** 和 **`std::shared_ptr`** 的適用場景。為什麼在單線程或明確所有權的 HFT 程式碼中，`unique_ptr` 幾乎總是首選？

Q5: 描述 C++ 中的 **`constexpr`** 關鍵字的作用。它如何幫助編譯器在**編譯期**完成計算，從而減少運行時延遲？

Q6: 比較 C++ 中 `static` 關鍵字在**局部變量**、**類成員變量**和**類成員函數**中的作用。

Q7: 簡述 C++ **Lambda 表達式**的幾種捕獲方式（`[]`, `[&]`, `[=]`, `[var]`）。在高併發環境下，哪種捕獲方式可能導致意想不到的線程安全問題？

Q8: 為什麼在高頻交易的核心路徑中，通常會**避免使用 C++ 異常 (Exceptions)**？您會如何設計錯誤碼或狀態檢查來替代異常處理？

Q9: 簡述常用的 **編譯器優化** 選項（如 `-O3`, `-march=native`）對 C++ 網路程式的影響。

Q10: 描述 **`inline`** 關鍵字的建議用途和局限性。為什麼在編譯器優化級別高時，手動使用 `inline` 可能並不總是有效？

### 性能與內存管理優化
Q1: 什麼是 **Cache Line Padding (緩存行填充)**？如何在 C++ 結構體中使用 `alignas` 或特定的編譯器擴展來進行緩存行對齊，以優化數據訪問？

Q2: 為什麼高頻交易系統需要實現自己的**內存池 (Memory Pool)**？請描述一種**固定大小塊內存池**的基本結構和優勢。

Q3: 描述 C++ 中的 **Placement New** 語法 (`new (ptr) T(...)`)。它在內存池或共享內存應用中是如何使用的？

Q4: 比較**棧 (Stack)** 和**堆 (Heap)** 分配的優缺點和開銷。在高頻交易中，應該盡可能將哪些變量放在棧上？

Q5: 解釋**空間局部性 (Spatial Locality)** 和**時間局部性 (Temporal Locality)**。在 C++ 代碼中，如何通過**數據結構佈局**和**迭代順序**來提高數據局部性？

Q6: 比較 **Protobuf**、**JSON** 和**原始二進制**序列化在高頻交易中的優缺點。您會如何設計一個**高效、低延遲**的二進制數據協議？

Q7: 描述 `volatile` 關鍵字的用途。它是否能保證線程安全？為什麼在多線程環境中，`std::atomic` 比 `volatile` 更為可靠？
### 併發與無鎖編程
Q1: 比較 **自旋鎖 (Spinlock)** 和 **互斥鎖 (Mutex)** 的性能開銷。什麼是**鎖爭用 (Lock Contention)**？在哪種情況下自旋鎖優於互斥鎖？

Q2: **死鎖 (Deadlock)** 和 **活鎖 (Livelock)** 有何區別？如何在高頻 C++ 併發設計中避免這兩類問題？

Q3: 描述 **讀寫鎖 (Reader-Writer Lock)** 的優勢。如果讀操作遠多於寫操作，它如何優於標準互斥鎖？

Q4: 描述 C++11 **`std::atomic`** 模板的作用。它如何通過提供原子操作來避免使用重量級鎖？

Q5: 描述 **CAS (Compare-and-Swap)** 原語的工作原理。它在實現無鎖數據結構（如無鎖隊列）中扮演什麼角色？

Q6: 簡述 **記憶體順序 (Memory Ordering)** 的概念。在 C++11 的 `std::atomic` 中，`std::memory_order_relaxed` 和 `std::memory_order_acquire/release` 的區別是什麼？

Q7: 描述 **無鎖隊列 (Lock-Free Queue)** 的基本結構（如 **Ring Buffer**）。在高頻交易中，它如何用於**跨線程數據傳輸**以最小化延遲？

Q8: 描述 **條件變數 (Condition Variable)** 的工作原理。在高性能 C++ 程式中，如何避免 **驚群效應 (Thundering Herd)** 問題？