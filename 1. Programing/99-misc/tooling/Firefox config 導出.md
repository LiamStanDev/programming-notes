1.  在地址列輸入：`about:support`
2. 找到「**設定檔資料夾（Profile Folder）**」，點選「**開啟資料夾**」
3. 關閉 Firefox（必須先完全關閉才能複製檔案）
4. 複製整個 `xxxxxxxx.default-release` 這類資料夾（名稱可能不同）
5. 儲存到 USB 或雲端
6. 另一台電腦, 開啟 `about:support` 找到當地的設定檔資料夾
7. 把原本的設定檔資料夾備份
8. 將原本複製的設定檔覆蓋進去（或用相同名稱取代）
9. 重啟 Firefox