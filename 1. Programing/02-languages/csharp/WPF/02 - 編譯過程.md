### 流程
1. **xaml 文檔被轉換成 baml 資源**，嵌入到 DLL 或者程序集中。
	* baml 是 xaml 的二進制形式，經過優化處理運行時能更快解析。
2. 創建類實例，也就是調用 `InitializeComponent()` 獲取 baml 資源，解析出控件對象並設置其屬性。
3. (Optional) 運行中動態加載資源集。 e.g. 用戶可以選擇主題。

