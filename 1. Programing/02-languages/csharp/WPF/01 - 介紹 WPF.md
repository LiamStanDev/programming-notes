### 與 Winform 有甚麼不同?
##### 撰寫方式
* winform 是使用 C# 來建構 UI 組件，而 WPF 是使用 xaml 來建構組件。
* Winform 是平面式布局，而 WPF 是依據 xaml 的聲明採用樹型的布局 (同 HTML)。

##### 事件模型 
- 使用一個稱為路由事件（Routed Event）的事件模型，這使得事件能夠在整個可視化樹狀結構中進行路由，方便事件處理。
- WinForms 使用簡單的事件模型，事件的處理較為直接。
