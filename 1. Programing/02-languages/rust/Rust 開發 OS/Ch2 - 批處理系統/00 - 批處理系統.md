### 介紹
Batch System 可以無需或少量用戶交互下即可運行程序，*可以自動安排程序執行*，也就是批處理作業，核心思想就是把多個程序打包一起輸入到計算機內。

### 隔離保護
應用程序難免會出錯，若應用程序出錯導致整個計算機系統無法運行這就非常糟糕，故我們希望應用程序出錯不要影響其他應用程序、操作系統與整個計算機系統。這需要操作系統能夠終止出錯的應用程序，轉而執行下一個應用程序，這種保護機制稱為*特權級 (Privilege)* 機制，讓應用程序跑在用戶態，而操作系統運行在內核態，實現隔離保護，需要硬件與軟件協同才能做到。
