### 概念
`tup` 可以存放多個不同類型，長度固定的類型，相較於 `struct`，元組好處不用命名，但壞處就是含意不夠清晰。

```rust
fn main() {
    let tup: (i32, f64, i32) = (500, 6.4, 1);

    let (x, y, z) = tup; // 使用模式匹配解構

    let k = tup.0; // 使用 . 進行訪問

    println!("{}, {}, {}. k = {}", x, y, z, k);
}
```

