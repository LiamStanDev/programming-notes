# 常用宏庫深度解析

> 基於 Rust 1.90+ (2025) | serde, thiserror, 等核心宏庫

## 📋 概述

Rust 生態中有許多優秀的宏庫。本章深入分析它們的實現原理和使用技巧。

---

## 🎯 serde 派生宏

### Serialize 和 Deserialize

```rust
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize)]
struct User {
    #[serde(rename = "username")]
    name: String,
    
    #[serde(skip_serializing_if = "Option::is_none")]
    email: Option<String>,
}
```

---

## 🔧 thiserror 錯誤處理

```rust
use thiserror::Error;

#[derive(Error, Debug)]
pub enum MyError {
    #[error("invalid input: {0}")]
    InvalidInput(String),
}
```

---

*最後更新: 2025-01-17*  
*Rust 版本: 1.90+*
